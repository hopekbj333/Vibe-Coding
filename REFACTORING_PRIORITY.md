# 🔧 리팩토링 우선순위 및 계획

**작성일**: 2025-01-XX  
**목적**: 가독성, 유지보수성, 효율성 향상을 위한 리팩토링 우선순위 정리

---

## 📊 프로젝트 현황 분석

### 파일 통계
- **총 Dart 파일**: 292개
- **가장 긴 파일**: 
  - `mock_assessment_repository.dart`: 1,182줄
  - `json_games_demo_page.dart`: 1,180줄
  - `training_home_page.dart`: 1,135줄
  - `assessment_player_page.dart`: 1,083줄
  - `story_question_page.dart`: 913줄

### 코드 품질 이슈
- **print 문**: 412개 (디버그 로그)
- **kDebugMode 체크**: 91개
- **에러 처리 블록**: 382개
- **TODO 주석**: 다수 발견

---

## 🎯 리팩토링 우선순위

### 🔴 **P0 - 최우선 (안정성 및 유지보수성)**

#### 1. 로깅 시스템 통합 및 정리
**우선순위**: ⭐⭐⭐⭐⭐  
**영향도**: 높음  
**예상 시간**: 2-3시간

**문제점**:
- `print()` 문이 412개 산재
- `kDebugMode` 체크가 91개 반복
- `_debugLog()` 함수가 특정 파일에만 존재
- 로그 형식이 일관되지 않음

**리팩토링 방안**:
```dart
// core/utils/logger.dart 생성
class AppLogger {
  static void debug(String message, {Map<String, dynamic>? data});
  static void info(String message, {Map<String, dynamic>? data});
  static void warning(String message, {Map<String, dynamic>? data});
  static void error(String message, {Object? error, StackTrace? stackTrace});
}
```

**대상 파일**:
- `story_question_page.dart` (50+ print 문)
- `tts_service.dart` (20+ print 문)
- `instruction_sequence_executor.dart` (30+ print 문)
- 모든 서비스 파일들

**예상 효과**:
- 로그 일관성 향상
- 프로덕션에서 로그 제거 용이
- 디버깅 효율성 향상

---

#### 2. 에러 처리 패턴 표준화
**우선순위**: ⭐⭐⭐⭐⭐  
**영향도**: 높음  
**예상 시간**: 3-4시간

**문제점**:
- 에러 처리 패턴이 파일마다 다름
- `try-catch` 블록이 382개 산재
- 에러 메시지 형식 불일치
- 에러 로깅이 일관되지 않음

**리팩토링 방안**:
```dart
// core/exceptions/app_exceptions.dart 확장
// core/utils/error_handler.dart 생성
class ErrorHandler {
  static Future<T> handleAsync<T>(Future<T> Function() action, {
    String? context,
    T? defaultValue,
  });
  
  static T handleSync<T>(T Function() action, {
    String? context,
    T? defaultValue,
  });
}
```

**대상 파일**:
- 모든 Repository 구현체
- 모든 Service 클래스
- 모든 Provider 클래스

**예상 효과**:
- 에러 처리 일관성 향상
- 에러 추적 용이
- 사용자 친화적 에러 메시지

---

#### 3. 긴 파일 분할 (1000줄 이상)
**우선순위**: ⭐⭐⭐⭐  
**영향도**: 중간  
**예상 시간**: 4-6시간

**문제점**:
- `mock_assessment_repository.dart`: 1,182줄
- `json_games_demo_page.dart`: 1,180줄
- `training_home_page.dart`: 1,135줄
- `assessment_player_page.dart`: 1,083줄
- `story_question_page.dart`: 913줄

**리팩토링 방안**:

**3.1 `story_question_page.dart` (913줄)**
- `_buildQuestionWidget()` → 별도 위젯 파일로 분리
- `_playQuestionAudio()` → `AudioPlaybackService`로 분리
- `_playFullInstructionSequence()` → 이미 분리됨 (유지)
- 스피커 버튼 로직 → `SpeakerButtonWidget`으로 분리

**3.2 `mock_assessment_repository.dart` (1,182줄)**
- Mock 데이터 → `mock_data/` 폴더로 분리
- 각 모듈별 Mock Repository로 분리

**3.3 `json_games_demo_page.dart` (1,180줄)**
- 데모 페이지는 개발용이므로 우선순위 낮음
- 필요시 게임별 데모 페이지로 분리

**3.4 `training_home_page.dart` (1,135줄)**
- 위젯들을 별도 파일로 분리
- `_buildProgressSection()` → `ProgressSectionWidget`
- `_buildGameGrid()` → `GameGridWidget`

**3.5 `assessment_player_page.dart` (1,083줄)**
- 위젯들을 별도 파일로 분리
- 상태 관리 로직 분리

**예상 효과**:
- 가독성 향상
- 테스트 용이성 향상
- 유지보수성 향상

---

### 🟠 **P1 - 높은 우선순위 (코드 품질)**

#### 4. 하드코딩된 값 상수화
**우선순위**: ⭐⭐⭐⭐  
**영향도**: 중간  
**예상 시간**: 2-3시간

**문제점**:
- 딜레이 시간: `1000`, `300`, `500` 등이 하드코딩
- 볼륨 값: `0.7`, `0.4` 등이 여러 곳에 산재
- 타임아웃 시간: `15초`, `10초` 등이 하드코딩
- 경로 문자열: `'assets/audio/environment/dog.mp3'` 등

**리팩토링 방안**:
```dart
// core/constants/audio_constants.dart
class AudioConstants {
  static const defaultVolume = 0.7;
  static const ttsVolume = 1.0;
  static const defaultDelayMs = 1000;
  static const shortDelayMs = 300;
  static const audioTimeoutSeconds = 15;
  static const ttsTimeoutSeconds = 10;
}

// core/constants/asset_paths.dart
class AssetPaths {
  static const audioEnvironment = 'assets/audio/environment/';
  static const audioPitch = 'assets/audio/pitch/';
  // ...
}
```

**대상 파일**:
- `story_question_page.dart`
- `instruction_sequence_executor.dart`
- `tts_service.dart`
- 모든 게임 파일들

---

#### 5. 중복 로직 추출
**우선순위**: ⭐⭐⭐⭐  
**영향도**: 중간  
**예상 시간**: 3-4시간

**문제점**:
- 오디오 재생 로직이 여러 곳에 중복
- TTS 초기화 로직 반복
- 에러 처리 패턴 반복
- 상태 체크 로직 반복

**리팩토링 방안**:

**5.1 오디오 재생 서비스 통합**
```dart
// core/services/audio_playback_service.dart
class AudioPlaybackService {
  Future<void> playAsset(String assetPath);
  Future<void> playSequence(List<String> assetPaths, {int delayBetween});
  Future<void> stop();
  Stream<PlayerState> get stateStream;
}
```

**5.2 TTS 초기화 공통화**
- 이미 싱글톤이지만, 초기화 로직을 더 명확하게

**5.3 상태 체크 유틸리티**
```dart
// core/utils/state_utils.dart
class StateUtils {
  static bool isMounted(BuildContext context);
  static void safeSetState(State state, VoidCallback fn);
}
```

---

#### 6. 매핑 데이터 JSON화
**우선순위**: ⭐⭐⭐  
**영향도**: 중간  
**예상 시간**: 2-3시간

**문제점**:
- `story_question_mapping_service.dart`에 35개 매핑이 하드코딩
- `storyContext`, `stageTitle` 등이 코드에 하드코딩
- TODO 주석: "JSON으로 이동 예정"

**리팩토링 방안**:
```json
// assets/config/ability_mappings.json
{
  "abilities": [
    {
      "abilityId": "0.1",
      "abilityName": "환경음 식별",
      "gameFileName": "environmental_sound.json",
      "gameTitle": "환경음 식별",
      "storyContext": "...",
      "stageTitle": "Stage 1-1: 기초 청각 능력"
    }
  ]
}
```

**대상 파일**:
- `story_question_mapping_service.dart` (457줄)

---

### 🟡 **P2 - 중간 우선순위 (코드 구조)**

#### 7. Provider 구조 개선
**우선순위**: ⭐⭐⭐  
**영향도**: 중간  
**예상 시간**: 2-3시간

**문제점**:
- Provider가 여러 파일에 분산
- Provider 간 의존성 불명확
- 상태 관리 패턴 불일치

**리팩토링 방안**:
- Provider를 feature별로 명확히 분리
- 의존성 그래프 문서화
- 공통 Provider 패턴 정의

---

#### 8. 모델 클래스 개선
**우선순위**: ⭐⭐⭐  
**영향도**: 낮음-중간  
**예상 시간**: 2-3시간

**문제점**:
- `freezed` 사용 불일치
- `copyWith` 메서드 수동 구현
- JSON 직렬화 패턴 불일치

**리팩토링 방안**:
- 모든 모델에 `freezed` 적용
- `json_serializable` 일관성 확보

---

#### 9. 위젯 재사용성 향상
**우선순위**: ⭐⭐  
**영향도**: 낮음-중간  
**예상 시간**: 3-4시간

**문제점**:
- 유사한 위젯이 여러 곳에 중복
- 공통 위젯이 `core/widgets/`에 있지만 활용도 낮음

**리팩토링 방안**:
- 공통 위젯 라이브러리 확장
- 위젯 사용 가이드 문서화

---

### 🟢 **P3 - 낮은 우선순위 (최적화)**

#### 10. 성능 최적화
**우선순위**: ⭐⭐  
**영향도**: 낮음  
**예상 시간**: 2-3시간

**문제점**:
- 불필요한 rebuild
- 메모리 누수 가능성
- 이미지/오디오 캐싱 최적화 필요

**리팩토링 방안**:
- `const` 위젯 활용
- `Provider` 최적화
- 캐싱 전략 개선

---

#### 11. 테스트 코드 추가
**우선순위**: ⭐⭐  
**영향도**: 낮음  
**예상 시간**: 지속적

**문제점**:
- 테스트 코드 부족
- 핵심 로직 테스트 필요

**리팩토링 방안**:
- Unit Test 추가
- Widget Test 추가
- Integration Test 추가

---

## 📋 우선순위별 작업 계획

### Phase 1: 안정성 기반 구축 (P0)
1. ✅ 로깅 시스템 통합 (2-3시간)
2. ✅ 에러 처리 표준화 (3-4시간)
3. ✅ 긴 파일 분할 - `story_question_page.dart` (2-3시간)

**총 예상 시간**: 7-10시간

### Phase 2: 코드 품질 향상 (P1)
4. ✅ 하드코딩 값 상수화 (2-3시간)
5. ✅ 중복 로직 추출 (3-4시간)
6. ✅ 매핑 데이터 JSON화 (2-3시간)

**총 예상 시간**: 7-10시간

### Phase 3: 구조 개선 (P2)
7. Provider 구조 개선 (2-3시간)
8. 모델 클래스 개선 (2-3시간)
9. 위젯 재사용성 향상 (3-4시간)

**총 예상 시간**: 7-10시간

### Phase 4: 최적화 (P3)
10. 성능 최적화 (2-3시간)
11. 테스트 코드 추가 (지속적)

---

## 🚨 주의사항

### 절대 수정 금지
- **1번, 2번 문항 관련 코드**: 사용자 명시적 승인 없이 절대 수정 금지
  - `instruction_sequences.json`의 "1", "2" 키
  - `story_question_page.dart`의 1-2번 문항 처리 로직
  - `instruction_sequence_executor.dart`의 `audio_sequence` 로직

### 리팩토링 원칙
1. **기능 변경 금지**: 리팩토링은 기능 변경 없이 구조만 개선
2. **점진적 진행**: 한 번에 하나씩, 테스트 후 다음 단계
3. **문서화**: 변경 사항 문서화 필수
4. **백업**: 리팩토링 전 커밋 필수

---

## 📝 체크리스트

### Phase 1 체크리스트
- [ ] 로깅 시스템 구현
- [ ] 모든 print 문을 AppLogger로 교체
- [ ] 에러 처리 유틸리티 구현
- [ ] story_question_page.dart 분할
- [ ] 테스트 및 검증

### Phase 2 체크리스트
- [ ] 상수 파일 생성
- [ ] 하드코딩 값 제거
- [ ] 오디오 재생 서비스 통합
- [ ] 매핑 데이터 JSON화
- [ ] 테스트 및 검증

---

## 🎯 예상 효과

### 가독성
- 파일 길이 평균 30% 감소
- 코드 복잡도 20% 감소
- 주석 및 문서화 향상

### 유지보수성
- 버그 수정 시간 40% 단축
- 새 기능 추가 시간 30% 단축
- 코드 리뷰 시간 25% 단축

### 효율성
- 빌드 시간 개선 (불필요한 코드 제거)
- 런타임 성능 개선 (최적화)
- 메모리 사용량 개선

---

*Last Updated: 2025-01-XX*
