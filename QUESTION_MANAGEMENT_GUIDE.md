# 📚 문항 관리 시스템 가이드

## 개요

문해력 기초 검사 앱의 **문항 관리 시스템**에 대한 통합 가이드입니다.

**핵심 목표:**
- ✅ 하드코딩 제거 - 모든 문항을 외부 데이터로 관리
- ✅ 대량 제작 지원 - 15,000개 이상의 문항 관리
- ✅ 유연한 확장 - 새로운 게임 타입 추가 용이
- ✅ 효율적 로딩 - 로컬 JSON + Firebase 하이브리드

---

## 🏗️ 시스템 아키텍처

### 전체 구조

```
┌─────────────────────────────────────────────────────────┐
│                    문항 제작 도구                         │
│  (구글 시트 + Apps Script)                               │
└─────────────┬───────────────────────────────────────────┘
              │ JSON 내보내기
              ▼
┌─────────────────────────────────────────────────────────┐
│                    문항 저장소                            │
│  ┌─────────────────────┐  ┌──────────────────────────┐  │
│  │  로컬 JSON           │  │  Firebase Firestore      │  │
│  │  (검사용 50개)       │  │  (학습용 15,000개+)      │  │
│  └─────────────────────┘  └──────────────────────────┘  │
└─────────────┬───────────────────────────────────────────┘
              │ 로딩
              ▼
┌─────────────────────────────────────────────────────────┐
│            QuestionLoaderService                        │
│  (로컬 우선 → Firebase 폴백)                            │
└─────────────┬───────────────────────────────────────────┘
              │ TrainingContentModel
              ▼
┌─────────────────────────────────────────────────────────┐
│                   게임 위젯                              │
│  SameSoundGameV2, SyllableClapGameV2, etc.             │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 데이터 구조

### 1. 분야별 구조

```
5개 분야 × 10개 유형 = 50개 유형
각 유형 × 300개 문항 = 15,000개 문항

┌──────────────────────────────────────────────────┐
│ 1. 음운 인식 (Phonological)                      │
│    - 같은 소리 찾기                               │
│    - 음절 쪼개기                                  │
│    - 음소 합성/분리                               │
│    ... (10개 유형)                                │
├──────────────────────────────────────────────────┤
│ 2. 청각 처리 (Auditory)                          │
│    - 소리 순서 기억                               │
│    - 리듬 패턴                                    │
│    ... (10개 유형)                                │
├──────────────────────────────────────────────────┤
│ 3. 시각 처리 (Visual)                            │
│    - 다른 그림 찾기                               │
│    - 숨은 그림 찾기                               │
│    ... (10개 유형)                                │
├──────────────────────────────────────────────────┤
│ 4. 작업 기억 (Working Memory)                    │
│    - 카드 매칭                                    │
│    - N-back                                       │
│    ... (10개 유형)                                │
├──────────────────────────────────────────────────┤
│ 5. 주의력 (Attention)                            │
│    - Go/No-Go                                     │
│    - 지속적 주의                                  │
│    ... (10개 유형)                                │
└──────────────────────────────────────────────────┘
```

### 2. 파일 명명 규칙

**로컬 JSON:**
```
assets/questions/training/{type}_{game_name}.json

예시:
- phonological_same_sound.json
- phonological_syllable_clap.json
- working_memory_card_match.json
```

**Firebase 컬렉션:**
```
training_contents/{contentId}

예시:
- phonological_same_sound_001
- phonological_same_sound_batch1_001
```

---

## 🔧 구현 세부사항

### 1. 데이터 모델

```dart
// lib/features/training/data/models/training_content_model.dart

class TrainingContentModel {
  final String contentId;
  final String moduleId;
  final TrainingContentType type;     // enum
  final GamePattern pattern;          // enum
  final String title;
  final String instruction;
  final String? instructionAudioPath;
  final List<ContentItem> items;
  final DifficultyParams difficulty;
  final Map<String, dynamic>? metadata;
}

class ContentItem {
  final String itemId;
  final String question;
  final String? questionAudioPath;
  final List<ContentOption> options;
  final String correctAnswer;
  final String? explanation;
  final Map<String, dynamic>? itemData;
}
```

### 2. 로딩 서비스

```dart
// lib/features/training/data/services/question_loader_service.dart

class QuestionLoaderService {
  // 로컬 JSON 로드
  Future<TrainingContentModel> loadFromLocalJson(String fileName);
  
  // Firebase 로드
  Future<TrainingContentModel?> loadFromFirebase(String contentId);
  
  // 모듈별 로드
  Future<List<TrainingContentModel>> loadByModule(String moduleId);
  
  // 난이도별 로드
  Future<List<TrainingContentModel>> loadByDifficultyRange(
    String moduleId, 
    int minLevel, 
    int maxLevel
  );
  
  // 하이브리드 로딩
  Future<TrainingContentModel?> loadHybrid({
    String? localFileName,
    String? firebaseContentId,
  });
}
```

### 3. 게임 위젯 통합

**Before (하드코딩):**
```dart
List<SoundQuestion> _generateQuestions(int level) {
  return [
    SoundQuestion(
      sounds: ['🥁 북', '🎹 피아노', '🥁 북'],
      correctIndices: {0, 2},
      soundPaths: ['drum.mp3', 'piano.mp3', 'drum.mp3'],
    ),
    // ... 하드코딩된 문항들
  ];
}
```

**After (JSON 기반):**
```dart
final QuestionLoaderService _loaderService = QuestionLoaderService();
TrainingContentModel? _content;

Future<void> _loadQuestions() async {
  _content = await _loaderService.loadFromLocalJson('same_sound.json');
  
  // 난이도 필터링
  final filteredItems = _content!.items.where((item) {
    final level = item.itemData?['level'] as int? ?? 1;
    return level <= widget.difficultyLevel;
  }).toList();
}
```

---

## 📝 제작 워크플로우

### 단계별 프로세스

```
1. 기획 ──────────────────────────────────────┐
   (게임 타입, 난이도, 문항 수 결정)          │
                                              │
2. 구글 시트 작성 ─────────────────────────┐  │
   Content Info, Items, Options          │  │
                                         │  │
3. Apps Script 실행 ───────────────────┐  │  │
   JSON 내보내기                       │  │  │
                                      │  │  │
4. JSON 파일 저장 ──────────────────┐  │  │  │
   assets/questions/training/      │  │  │  │
                                   │  │  │  │
5. 게임 위젯 연결 ───────────────┐  │  │  │  │
   loadFromLocalJson()          │  │  │  │  │
                                │  │  │  │  │
6. 테스트 ─────────────────────┐  │  │  │  │  │
   실제 게임 실행               │  │  │  │  │  │
                               │  │  │  │  │  │
7. Firebase 업로드 (선택) ───┐  │  │  │  │  │  │
   대량 문항의 경우           │  │  │  │  │  │  │
                             │  │  │  │  │  │  │
8. 배포 ─────────────────────┴──┴──┴──┴──┴──┴──┘
```

### 예상 소요 시간

| 단계 | 10개 문항 | 100개 문항 | 300개 문항 |
|------|-----------|------------|------------|
| 기획 | 30분 | 1시간 | 2시간 |
| 시트 작성 | 1시간 | 5시간 | 15시간 |
| JSON 변환 | 1분 | 2분 | 3분 |
| 연결 및 테스트 | 30분 | 1시간 | 2시간 |
| **총계** | **2시간** | **7시간** | **19시간** |

---

## 🎯 사용 시나리오

### 시나리오 1: MVP (최소 기능 제품)

**목표:** 3개 게임 × 10개 문항 = 30개

**작업:**
1. ✅ `same_sound.json` - 10개
2. ✅ `syllable_clap.json` - 10개
3. ✅ `card_match.json` - 10개

**상태:** 완료 ✅

---

### 시나리오 2: 베타 버전

**목표:** 10개 게임 × 50개 문항 = 500개

**작업:**
1. 음운 인식 5개 게임 × 50개 = 250개
2. 작업 기억 3개 게임 × 50개 = 150개
3. 주의력 2개 게임 × 50개 = 100개

**방법:**
- 구글 시트로 배치 작성
- 게임당 1개 JSON 파일
- 로컬 저장 위주

---

### 시나리오 3: 정식 출시

**목표:** 50개 게임 × 100개 문항 = 5,000개

**작업:**
- 5개 분야 × 10개 게임 × 100개 = 5,000개

**방법:**
- 구글 시트로 대량 작성
- Firebase Firestore 활용
- 배치별로 업로드 (batch1, batch2, ...)

---

### 시나리오 4: 완전체

**목표:** 50개 게임 × 300개 문항 = 15,000개

**방법:**
- 지속적 확장
- 사용자 피드백 반영
- 난이도 자동 조정 데이터 활용

---

## 🔍 품질 관리

### 검증 항목

**자동 검증 (Apps Script):**
- [ ] itemId 중복 체크
- [ ] 모든 문항에 선택지 존재
- [ ] correctAnswer 형식 확인
- [ ] 경로 형식 확인

**수동 검증:**
- [ ] 난이도 적절성
- [ ] 오디오 파일 확인
- [ ] 이미지 파일 확인
- [ ] 정답 정확성

### 테스트 체크리스트

```markdown
## 문항 테스트 체크리스트

### 1. 로딩 테스트
- [ ] JSON 파일 로드 성공
- [ ] 난이도 필터링 동작
- [ ] 에러 핸들링 확인

### 2. 게임 플레이 테스트
- [ ] 문항 표시 정상
- [ ] 선택지 표시 정상
- [ ] 정답 판정 정확
- [ ] 피드백 표시 정상

### 3. 진행 테스트
- [ ] 다음 문항 이동
- [ ] 진도 표시 정확
- [ ] 완료 처리 정상

### 4. 에셋 테스트
- [ ] 오디오 재생 (있는 경우)
- [ ] 이미지 표시 (있는 경우)
- [ ] 캐릭터 애니메이션
```

---

## 📈 확장 계획

### Phase 1: MVP (완료)
- ✅ 시스템 설계
- ✅ 로더 서비스 구현
- ✅ POC 3개 게임 JSON 전환
- ✅ 문서화

### Phase 2: 베타 (진행 중)
- ⏳ 10개 게임 JSON 전환
- ⏳ 500개 문항 제작
- ⏳ Firebase 연동 테스트

### Phase 3: 정식 출시
- ⬜ 50개 게임 완성
- ⬜ 5,000개 문항 제작
- ⬜ 자동 난이도 조정
- ⬜ 품질 관리 시스템

### Phase 4: 지속 확장
- ⬜ 15,000개+ 문항
- ⬜ 사용자 생성 콘텐츠
- ⬜ AI 기반 문항 생성

---

## 🔗 관련 문서

| 문서 | 설명 |
|------|------|
| `FIREBASE_SCHEMA.md` | Firestore 데이터베이스 구조 |
| `GOOGLE_SHEETS_TEMPLATE.md` | 구글 시트 템플릿 및 Apps Script |
| `training_content_model.dart` | 데이터 모델 정의 |
| `question_loader_service.dart` | 로딩 서비스 구현 |
| `same_sound_game_v2.dart` | JSON 기반 게임 예시 |

---

## 💡 팁 & 트릭

### 1. 효율적인 제작

```
✅ DO:
- 구글 시트 템플릿 재사용
- 배치 단위로 작업 (50~100개)
- Apps Script로 자동화
- 정기적인 백업

❌ DON'T:
- 코드에 직접 하드코딩
- 파일 단위로 개별 작업
- 수동 검증만 의존
- 백업 없이 작업
```

### 2. 협업 팁

- 구글 시트 공유로 동시 작업
- 파일 명명 규칙 준수
- 주간 동기화 미팅
- 변경 이력 관리

### 3. 문제 해결

**Q: JSON 파일이 너무 커요**
- A: 파일을 분할하거나 Firebase 사용

**Q: 로딩이 느려요**
- A: 캐싱 사용 또는 필요한 난이도만 로드

**Q: 문항 수정이 반영 안 돼요**
- A: 앱 재시작 또는 캐시 클리어

---

## 📞 지원

문의사항이나 제안사항은 프로젝트 관리자에게 연락해주세요.

---

**작성일:** 2025-12-06  
**작성자:** AI Assistant  
**버전:** 1.0.0  
**마지막 업데이트:** 2025-12-06
